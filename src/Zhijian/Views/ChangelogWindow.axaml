<atom:Window xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:atom="using:AtomUI.Desktop.Controls"
             xmlns:codewf="https://codewf.com"
             xmlns:vm="using:Zhijian.ViewModels"
             x:Class="Zhijian.Views.ChangelogWindow"
             x:DataType="vm:ChangelogWindowViewModel"
             WindowStartupLocation="CenterOwner"
             Title="更新日志"
             Width="840"
             Height="680"
             MinWidth="640"
             MinHeight="520">

    <Design.DataContext>
        <vm:ChangelogWindowViewModel />
    </Design.DataContext>

    <atom:Window.Styles>
        <StyleInclude Source="/Styles/ChangelogWindow.axaml" />
    </atom:Window.Styles>

    <Border Padding="24,20">
        <Grid RowDefinitions="Auto,*" RowSpacing="14">

            <StackPanel Orientation="Horizontal"
                        Spacing="8"
                        HorizontalAlignment="Right">
                <atom:Button Content="中文"
                             MinWidth="74"
                             Height="30"
                             Padding="10,4"
                             Command="{Binding LoadChineseAsync}" />
                <atom:Button Content="English"
                             MinWidth="74"
                             Height="30"
                             Padding="10,4"
                             Command="{Binding LoadEnglishAsync}" />
            </StackPanel>

            <ScrollViewer Grid.Row="1"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
				<codewf:MarkdownViewer Classes="changelogMarkdown"
                                       Markdown="{Binding Markdown}"
                                       TypographyTheme="{x:Static codewf:MarkdownTypographyThemes.Simple}"
                                       TypographySize="{x:Static codewf:MarkdownTypographySizes.Small}" />
            </ScrollViewer>
        </Grid>
    </Border>
</atom:Window>
